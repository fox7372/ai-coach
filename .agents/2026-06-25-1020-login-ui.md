# 2026-06-25 10:20

## Issue

- The login page looked broken because backend port `8000` was not running.
- The old login/register error message was too generic, so backend connection failures were not clear.

## Changes Made

- Added login/register loading state.
- Added empty username/password validation.
- Added clear frontend messages for backend not started and backend timeout.
- Trimmed username before sending auth requests.

## Verification

- Started FastAPI backend on `127.0.0.1:8000`.
- Verified `/health` returns `status=ok`, `database=connected`.
- Verified browser login with the default `demo/demo123` account enters the course list.
- Ran frontend TypeScript build check with `tsc -b`.
