# 2026-06-25 推荐资料只打开直接网址

## 背景

用户指出“打开网址”应直接进入资料页面，而不是打开搜索引擎结果。

## 修改

- 后端取消推荐资料网址的搜索页兜底。
- AI 未返回 `http/https` 直接网址时，后端保留空网址。
- 前端仅在存在直接网址时显示“打开网址”。
- 没有直接网址时显示“暂无直接网址”，仍保留搜索关键词供用户自行判断。

## 验证

- 已运行 `python -m py_compile backend/app/main.py`。
- 已运行前端 TypeScript 构建检查：`tsc -b` 通过。
