# 2026-06-25 AI 推荐资料加入课程

## 背景

用户指出 AI 推荐课程资料后，推荐项应该可以加入课程，而不是只展示给用户看。

## 修改

- 前端在 AI 推荐创建课程后记录课程 ID。
- 推荐资料卡片中，如果存在直接网址，显示“加入课程”按钮。
- 点击“加入课程”会调用网页资料导入接口，将该网址作为当前推荐课程的资料加入。
- 加入成功后从推荐清单中移除该项，并在导入状态中显示新资料。
- 没有直接网址的推荐仍不能自动加入，提示用户先找到真实页面再导入。

## 验证

- 已运行前端 TypeScript 构建检查：`tsc -b` 通过。
