# 2026-06-25 前端视觉升级

## 背景

用户要求安装 GitHub 上的 taste skill，并按后端已有功能修改前端画面，不能删除功能。

## 安装

- 已从 `https://github.com/Leonxlnx/taste-skill` 手动安装：
  - `design-taste-frontend`
  - `redesign-existing-projects`
- 本机没有 `npx` 命令，因此采用直接下载 `SKILL.md` 到 `C:\Users\fox\.codex\skills` 的方式。
- 新 skill 在后续会话中通常需要重启 Codex 才会自动出现在技能列表里。

## 修改

- 保留所有原有功能和接口入口。
- 重做登录页视觉层级，突出平台学习闭环。
- 重做主应用外壳，包括侧边栏、顶部栏、内容宽度和背景层次。
- 重做课程列表卡片，强调课程独立数据、进度和掌握度。
- 重做课程详情顶部和子目录标签，降低拥挤感。
- 重做上传/导入页的信息结构，强化 AI 推荐创建课程、PDF、视频、网页导入和导入状态。
- 新增统一面板、按钮、输入框、焦点、按压反馈和背景质感样式。

## 验证

- 已运行前端 TypeScript 构建检查：`tsc -b` 通过。
- 已运行 `vite build` 通过。
- 已确认前端 `http://127.0.0.1:5173` 返回 200。
- 已确认后端 `http://127.0.0.1:8000/health` 返回数据库连接正常。
- 未额外安装项目依赖。
