# 2026-06-25 课程数据隔离

## 背景

用户指出每个课程的测试题和错题库都应该是不一样的，不要连接到一起。

## 修改

- 检查后端生成测验接口，确认请求和保存都已使用 `course_id`。
- 修复前端课程切换时仍保留上一门课页面状态的问题。
- 切换课程时清空资料、知识点、学习计划、测试题、错题、诊断和画像等课程态，等待当前课程重新加载。
- 测验页面显示当前课程名称，并在未生成时提示只显示当前课程题目。

## 验证

- 已运行前端 TypeScript 构建检查：`tsc -b` 通过。
